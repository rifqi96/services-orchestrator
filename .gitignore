# =============================================================================
# instance state (CLI-managed, per-VPS) — never commit
# =============================================================================
services.yml
services.yml.bak
services.yml.*.bak
.env
.env.local

# =============================================================================
# certificates & keys — NEVER commit private key material
# (broad catch-all; the only allowed .pem is the public DH params)
# =============================================================================
*.pem
*.key
!nginx/tls/ssl-dhparams.pem

# =============================================================================
# generated / runtime artifacts — keep the dirs, ignore the contents
# =============================================================================
nginx/conf.d/*
!nginx/conf.d/.gitkeep
nginx/.conf.d.tmp/
nginx/.networks
docker-compose.networks.yml
nginx/certbot/conf/*
!nginx/certbot/conf/.gitkeep
nginx/certbot/www/*
!nginx/certbot/www/.gitkeep
nginx/certbot/logs/*
!nginx/certbot/logs/.gitkeep
nginx/logs/*
!nginx/logs/.gitkeep

# =============================================================================
# misc
# =============================================================================
.DS_Store
*.log
CLAUDE.md
tasks/
task_research/
.cursor/
memory-bank/
